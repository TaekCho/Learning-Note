import express from "express";
const app = express();
const port = 3000;

app.listen(port, () => {
  console.log(`Server running on port ${port}.`);
});

        Get => request resource
        Post => sending resource
update  Put => replace resource
update  Patch => patch up a resource
        Delete => delete resource
